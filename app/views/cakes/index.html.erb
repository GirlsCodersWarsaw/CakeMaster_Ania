<table>
  <tr>
    <th>Nazwa ciasta</th>
    <th>Rodzaj ciasta</th>
    <th>Przepis</th>
  </tr>
  <% @cakes.each do |cake| %>
      <tr>
        <td><%= cake.name %></td>
        <td><%= cake.kind %></td>
        <td><%= cake.description %></td>
      </tr>
  <% end %>
</table>

